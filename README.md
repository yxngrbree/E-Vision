# 🌿 E-Vision

E-Vision is a tool built to explore real-time emotion recognition using Python. It identifies facial expressions and provides a simple visual dashboard for data observation.

## 🛠️ Requirements
* **Python 3.10+**
* **Webcam**
* **System Libraries**: `sudo apt install python3-tk` (Linux only)

## 🚀 Setup
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
